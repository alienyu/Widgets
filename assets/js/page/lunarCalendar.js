var LunarCalendar = require("../widgets/lunarCalendar/lunarCalendar.js");

var fuc = {
    config: {},
    init: function() {

    },
    init: function() {
        new LunarCalendar({
            cyear: "2016",
            cmonth: "10"
        })
    }
}